import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./styles.css";

// Render the app into the root div from index.html.
ReactDOM.createRoot(document.getElementById("root")).render(<App />);
